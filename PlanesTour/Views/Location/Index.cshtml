@model PlanesTour.ViewModels.Location.Index

<div class="row">

    @foreach (var location in Model.Locations)
    {
        <div class="col-md-4 portfolio-item">
            <a href="#">
               @* <img class="img-responsive" src="@location.Photos.FirstOrDefault()" alt="">*@
            </a>
            <p>@location.Name</p>
        </div>
    }
</div>

